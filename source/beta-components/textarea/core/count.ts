/**
 * Re-exported so the character count a person sees is identical in every
 * control that shows one. The implementation lives in `shared/core/text.ts`.
 */
export { countCharacters } from "../../shared/core/text";
