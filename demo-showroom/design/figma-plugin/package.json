{
  "name": "grampro-kit-figma-generator",
  "private": true,
  "version": "1.0.0",
  "description": "Figma plugin that generates the Grampro Kit design system file: variables, text styles and component sets.",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "watch": "tsc -p tsconfig.json --watch",
    "smoke": "node test/smoke.mjs"
  },
  "devDependencies": {
    "@figma/plugin-typings": "^1.100.0",
    "typescript": "~5.8.0"
  }
}
